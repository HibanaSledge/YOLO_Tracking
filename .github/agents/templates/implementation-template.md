# Implementation Template

用于代码或 agent 配置实现后的总结。必须说明影响范围、兼容性、验证和证据缺口。

## 变更目标

- 用户需求：
- 实际交付：
- 不包含内容：

## 修改文件

- 新增：
- 更新：
- 删除：
- 未改但相关：

## 设计约束

- 保持 CLI 兼容：是 / 否 / 不适用
- 保持 summary 字段兼容：是 / 否 / 不适用
- 保持 frame_metrics 字段兼容：是 / 否 / 不适用
- 保持 performance 字段兼容：是 / 否 / 不适用
- 是否影响质量参数：是 / 否
- 是否影响实时 latest-frame 架构：是 / 否
- 是否需要更新 `lock_target_change_log.md`：是 / 否

## 实现摘要

- 核心改动：
- 复用的既有模式：
- 新增的行为：
- 移除或回退的行为：

## 项目语义检查

- tracker id 与业务目标是否仍分离：是 / 否 / 不适用
- FACE_LOCK 与 HEAD_PROXY 是否仍清晰区分：是 / 否 / 不适用
- 性能与质量是否分开描述：是 / 否 / 不适用

## 验证结果

- 静态检查：
- 运行验证：
- 相关测试：
- 文档链接验证：
- 关键指标：

## 风险

- 潜在回归：
- 边界场景：
- 性能影响：
- 质量影响：

## 未验证项与补证方式

- 未验证项：
- 原因：
- 建议补证命令或产物：